# 🏗️ EDGE FUNCTIONS ARCHITECTURE DIAGRAM

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                       │
│                        🌍 INTERNET / USERS                           │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ↓                  ↓                  ↓
    ┌────────┐        ┌────────┐        ┌────────┐
    │ React  │        │ React  │        │ React  │
    │ Web    │        │ Mobile │        │ Admin  │
    │ App    │        │ App    │        │ Panel  │
    └────┬───┘        └────┬───┘        └────┬───┘
         │                 │                 │
         └─────────────────┼─────────────────┘
                           │
                    API / WebSocket
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ↓                  ↓                  ↓
    ┌────────────────────────────────────────────┐
    │     Supabase PostgreSQL Database           │
    ├────────────────────────────────────────────┤
    │  Tables:                                   │
    │  • matches (id, teams, score, status...)   │
    │  • standings (league_id, standings_data)   │
    │  • feeds (content, type, source)           │
    │  • profiles, bets, odds...                 │
    │  • RLS policies for security               │
    └────────────────────────────────────────────┘
            │
            │ upsert/insert via service role
            │
            ↑
        ┌───┴──────────────────────────┐
        │                              │
        ↓                              ↓
┌─────────────────────┐      ┌─────────────────────┐
│  Edge Functions     │      │  Scheduled Jobs     │
│  (Deno Runtime)     │      │  (GitHub Actions)   │
├─────────────────────┤      ├─────────────────────┤
│ fetch-matches       │←─────│ Cron: */30 * * * *  │
│ fetch-standings     │←─────│ Cron: 0 2 * * *    │
│ fetch-scorers       │←─────│ Cron: 0 */12 * * * │
└──┬──────────────────┘      └─────────────────────┘
   │ fetch + rate limit
   │
   └─ Rate limit: 10 calls/min
   └─ Error handling + logging
   └─ CORS enabled
   │
   ↓
┌─────────────────────────────────────────────┐
│      API-Football (RapidAPI)                │
├─────────────────────────────────────────────┤
│ GET /fixtures?live=all                      │
│ GET /fixtures?from=DATE&to=DATE&status=NS  │
│ GET /standings?league=39&season=2024        │
│ GET /players/topscorers?league=39           │
│ GET /teams?id=XXX                           │
│                                             │
│ Quota: 10 calls/min (free tier)             │
└─────────────────────────────────────────────┘
        │
        ├─ Live match data
        ├─ Upcoming fixtures
        ├─ League standings
        ├─ Top scorers
        └─ Team information
```

---

## Data Flow Sequence

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                        │
│  1. SCHEDULED TRIGGER (GitHub Actions)                               │
│     ├─ Every 30 min: Fetch matches                                   │
│     ├─ Every 12 hours: Fetch scorers                                 │
│     └─ Daily 2 AM: Fetch standings                                   │
│                                                                        │
│  2. EDGE FUNCTION EXECUTES (Deno)                                    │
│     ├─ Download function from registry                               │
│     ├─ Initialize Deno runtime (~50ms cold start)                    │
│     ├─ Load environment variables                                    │
│     └─ Execute TypeScript code                                       │
│                                                                        │
│  3. API-FOOTBALL FETCH (RapidAPI)                                    │
│     ├─ Check rate limit: 10 calls/min ✅                             │
│     ├─ Set headers: x-rapidapi-key, x-rapidapi-host                 │
│     ├─ Make HTTP GET request                                         │
│     ├─ Handle response: 200, 401, 403, 429...                       │
│     └─ Parse JSON data                                               │
│                                                                        │
│  4. DATABASE INSERT (Supabase)                                       │
│     ├─ Use service_role for elevated privileges                      │
│     ├─ UPSERT matches data (merge with existing)                     │
│     ├─ INSERT standings as JSONB                                     │
│     ├─ INSERT feeds with stats content                               │
│     ├─ Trigger RLS policies                                          │
│     └─ Update updated_at timestamps                                  │
│                                                                        │
│  5. LOGGING & MONITORING                                             │
│     ├─ Console.log to Supabase function logs                         │
│     ├─ Track execution time                                          │
│     ├─ Report success/failure status                                 │
│     └─ Send to observability service (optional)                      │
│                                                                        │
│  6. FRONTEND SUBSCRIPTION                                            │
│     ├─ useRealtimeUpdates() hook activated                           │
│     ├─ React component receives new data                             │
│     ├─ UI re-renders with fresh matches/standings/scorers            │
│     └─ Users see live data! 🎉                                       │
│                                                                        │
└──────────────────────────────────────────────────────────────────────┘
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND LAYER                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ Pages/Components                                          │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ • ScoresPage ────────────┐                               │ │
│  │ • BetSlipPage ───────────┤                               │ │
│  │ • LeaderboardPage ───────┤                               │ │
│  │ • ExplorePage ───────────┤                               │ │
│  │ • Feed ──────────────────┤                               │ │
│  └───────────────────────────────────────────────────────────┘ │
│                            │                                    │
│                            ↓                                    │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ React Hooks (context/useSportsData.ts)                    │ │
│  ├───────────────────────────────────────────────────────────┤ │
│  │ • useLiveMatches() ─────────────→ query matches table    │ │
│  │ • useUpcomingMatches() ────────→ filter by kickoff_time  │ │
│  │ • useLeagueStandings() ────────→ standings table + JSON  │ │
│  │ • useTopScorers() ─────────────→ feeds table + parse     │ │
│  │ • useTriggerSync() ────────────→ POST to edge function   │ │
│  │                                                           │ │
│  │ Real-time subscriptions via:                             │ │
│  │ → supabase.from('matches').on('*', callback)             │ │
│  └──────────────────────┬──────────────────────────────────┘ │
│                         │                                      │
└─────────────────────────┼──────────────────────────────────────┘
                          │
                   Supabase Client
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ↓                 ↓                 ↓
    SELECT          REALTIME SUB        POST
    QUERY           BROADCAST           EDGE FN
        │                 │                 │
┌───────┴─────────────────┼─────────────────┴────────┐
│                         │                          │
└─────────────────────────┼──────────────────────────┘
                          │
                          ↓
        ┌─────────────────────────────────────┐
        │   Supabase Realtime Server          │
        │   (WebSocket)                       │
        └────────────────┬────────────────────┘
                         │
             ┌───────────┴───────────┐
             │                       │
             ↓                       ↓
        ┌─────────────┐          ┌──────────────┐
        │ Auth Layer  │          │ Database     │
        │             │          │ (PostgreSQL) │
        │ • JWT token │          │              │
        │ • RLS check │          │ • matches    │
        │ • Session   │          │ • standings  │
        └─────────────┘          │ • feeds      │
                                 └──────────────┘
```

---

## Edge Function Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    YOUR GITHUB REPOSITORY                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  supabase/                                                      │
│  ├─ functions/                                                 │
│  │  ├─ fetch-matches/                                         │
│  │  │  ├─ index.ts (165 lines)                               │
│  │  │  └─ deno.json (optional)                               │
│  │  ├─ fetch-standings/                                       │
│  │  │  ├─ index.ts (120 lines)                               │
│  │  │  └─ deno.json (optional)                               │
│  │  └─ fetch-scorers/                                         │
│  │     ├─ index.ts (130 lines)                               │
│  │     └─ deno.json (optional)                               │
│  │                                                             │
│  └─ tsconfig.json (TypeScript config)                         │
│                                                                 │
│  .github/                                                      │
│  └─ workflows/                                                 │
│     └─ schedule-*.yml  ←─ SCHEDULED TRIGGERS                  │
│                                                                 │
└────────────────┬───────────────────────────────────────────────┘
                 │
         git push origin main
                 │
                 ↓
    ┌────────────────────────────────┐
    │  GitHub Actions Workflow        │
    │  Triggered by:                  │
    │  • schedule (cron)              │
    │  • manual (workflow_dispatch)   │
    │  • push (optional)              │
    └─────────────┬────────────────────┘
                  │
                  ↓
    ┌────────────────────────────────┐
    │  1. Checkout code               │
    │  2. Setup environment           │
    │  3. Call edge function via URL  │
    │     ├─ https://<project>.supabase.co
    │     │   /functions/v1/fetch-matches
    │     │                            │
    │     └─ BEARER: ANON_KEY          │
    │                                  │
    └─────────────┬────────────────────┘
                  │
                  ↓
    ┌────────────────────────────────┐
    │  Supabase Edge Function         │
    │  Infrastructure                 │
    │  • Deno runtime v1.x            │
    │  • Global CDN edge locations    │
    │  • Automatic scaling            │
    │  • Auto-deployment on push      │
    │                                  │
    └─────────────┬────────────────────┘
                  │
                  ↓
    ┌────────────────────────────────┐
    │  Function Execution             │
    │  • Load environment variables   │
    │  • Rate limit check             │
    │  • Fetch from API-Football      │
    │  • Transform data               │
    │  • Insert to database           │
    │  • Log results                  │
    │                                  │
    └─────────────┬────────────────────┘
                  │
                  ↓
    ┌────────────────────────────────┐
    │  Supabase PostgreSQL            │
    │  • UPSERT matches               │
    │  • INSERT standings             │
    │  • INSERT feeds                 │
    │  • Update timestamps            │
    │                                  │
    └────────────────────────────────┘
```

---

## Real-Time Update Flow

```
         ┌──────────────────────────────────────┐
         │   Edge Function Completes Insert     │
         │   (trigger: UPDATE matches table)    │
         └────────────────┬─────────────────────┘
                          │
                          ↓
         ┌──────────────────────────────────────┐
         │  PostgreSQL Event Trigger            │
         │  (publish to replication slot)       │
         └────────────────┬─────────────────────┘
                          │
                          ↓
         ┌──────────────────────────────────────┐
         │  Supabase Realtime Server            │
         │  (processes change events)           │
         └────────────────┬─────────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
         ↓                ↓                ↓
    ┌──────┐         ┌──────┐         ┌──────┐
    │User │         │User  │         │User  │
    │  1  │         │  2   │         │  3   │
    └──┬───┘         └──┬───┘         └──┬───┘
       │                │                │
       ├────────────────┼────────────────┤
       │                │                │
       ↓                ↓                ↓
    WebSocket         WebSocket       WebSocket
    connection        connection      connection
       │                │                │
       └────────────────┼────────────────┘
                        │
              ┌─────────┴─────────┐
              │                   │
    React Component         React Component
    Re-renders              Re-renders
              │                   │
              ↓                   ↓
    ┌───────────────────┐  ┌───────────────────┐
    │ New live matches  │  │ New scorers data  │
    │ displayed in UI   │  │ displayed in UI   │
    └───────────────────┘  └───────────────────┘
```

---

## Scheduled Execution Timeline

```
Timeline (24-hour cycle):

00:00 ├─ 🏆 FETCH STANDINGS (fetch-standings)
      │  └─ All 5 leagues + save JSON
      │
00:30 ├─ 🔴 FETCH MATCHES (fetch-matches)
      │  └─ Live + upcoming for 7 days
      │
01:00 ├─ 🔴 FETCH MATCHES
      │
01:30 ├─ 🔴 FETCH MATCHES
      │
02:00 ├─ ⚽ FETCH SCORERS (fetch-scorers)
      │  └─ Top 10 from each league
      │
02:30 ├─ 🔴 FETCH MATCHES
      │
...  │ (repeats every 30 min for matches)
      │
12:00 ├─ ⚽ FETCH SCORERS (every 12 hours)
      │
14:00 ├─ More matches every 30 min
      │
...   │
      │
23:30 └─ 🔴 FETCH MATCHES (last of day)

Total daily:
• Matches: 48 calls (every 30 min)
• Standings: 1 call (daily)
• Scorers: 2 calls (12-hourly)
= 51 API calls/day
= ~1530 API calls/month
= Well within 10,000 calls/min quota ✅
```

---

## Database Schema Relationships

```
┌──────────────────────────────────────────────────────────────┐
│                  PostgreSQL Schema                           │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐        populated by:        ┌──────────────┐
│   matches       │◄────── fetch-matches        │ Edge Func 1  │
├─────────────────┤                             └──────────────┘
│ id (PK)         │
│ home_team       │  ────┐
│ away_team       │      │
│ kickoff_time    │  ────┼─→ Used by betting system
│ status          │      │
│ score           │  ────┘
│ odds_home/draw  │
│ odds_away       │
│ league          │
│ created_at      │
│ updated_at      │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│   standings     │◄────── fetch-standings     │ Edge Func 2  │
├─────────────────┤                            └──────────────┘
│ id (PK)         │
│ league_id (FK)  │
│ standings_data  │ ──→ JSONB array of teams with:
│ created_at      │     • rank, points, won, lost, etc
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│   feeds         │◄────── fetch-scorers      │ Edge Func 3  │
├─────────────────┤                            └──────────────┘
│ id (PK)         │
│ title           │ (e.g., "Top Scorers - Premier League")
│ content         │ (JSON array of scorers)
│ source          │ (API-Football, etc)
│ type            │ (stats, news, alert)
│ created_at      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│   bets          │◄────── User places bet on:
├─────────────────┤        • match_id (FK → matches)
│ id (PK)         │        • odds_selected
│ user_id (FK)    │        • amount
│ match_id (FK)   │◄────── populated from matches table
│ odds_selected   │
│ amount          │
│ status          │ (pending, won, lost)
│ result          │
└─────────────────────────────────────────────────────────────┘

Relationships:
matches ──1:N──→ bets (user bets on matches)
matches ──1:N──→ odds (multiple odds per match)
standings ◄──── league (identified by league_id)
feeds ◄────────── scorers (stored as JSON content)
```

---

## Cost & Performance Analysis

```
┌─────────────────────────────────────────────────────────────┐
│                    COST BREAKDOWN                           │
└─────────────────────────────────────────────────────────────┘

Supabase Free Tier:
├─ Edge Functions: 125K invocations/month ✅ (we use ~2K)
├─ Database writes: 500K/month ✅ (we use ~5K)
├─ Database reads: unlimited ✅
├─ Storage: 1GB ✅ (we use <100MB)
├─ Realtime: included ✅
└─ Total cost: $0 ✅

API-Football RapidAPI (Free Tier):
├─ Requests: 100/day ✅ (we use ~51/day)
├─ Rate limit: 10/min ✅ (we enforce in code)
├─ Cost: Free ✅
└─ To upgrade: $5/month for 3000 requests/day

GitHub Actions (Public Repo):
├─ Monthly minutes: Unlimited ✅
├─ Cost: Free ✅

Total Monthly Cost: $0 ✅

┌─────────────────────────────────────────────────────────────┐
│              PERFORMANCE TARGETS                            │
└─────────────────────────────────────────────────────────────┘

Metric                      Target      Expected    Status
─────────────────────────────────────────────────────────────
Edge function latency       <1s         ~500ms      ✅
Database insert time        <100ms      ~50ms       ✅
API call time               <5s         ~2s         ✅
Total sync time             <10s        ~3-5s       ✅
Data freshness              30min       30min       ✅
Concurrent syncs            unlimited   3 async     ✅
Uptime SLA                  99%         99.9%       ✅

```

---

**This architecture ensures your sports betting platform has live, real-time data with zero infrastructure management!** ⚡
